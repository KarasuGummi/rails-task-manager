<div class="container">
  <h1 class="title">My Tasks</h1>
  <div class="container">
    <ul>
      <% @tasks.each do |task| %>
        <li><%= link_to task.title, task_path(task) %></li>
      <% end %>
    </ul>
  </div>
  <p class="new-task">Add a new task</p>
</div>
